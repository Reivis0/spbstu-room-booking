services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: booking-frontend
    ports:
      - "3000:80"
    environment:
      - REACT_APP_API_URL=/api/v1
    networks:
      - booking-network
    restart: unless-stopped

networks:
  booking-network:
    driver: bridge

