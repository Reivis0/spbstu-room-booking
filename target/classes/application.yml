spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booking_db
    username: postgres
    password: 09062005
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # ????????????? ??????? ???????
    show-sql: true       # ?????????? SQL ? ???????
    properties:
      hibernate:
        format_sql: true

  redis:
    host: localhost
    port: 6379

server:
  port: 8080

