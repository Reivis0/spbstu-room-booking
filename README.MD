# 🏛️ Сервис бронирования аудиторий СПбПУ

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Docker](https://img.shields.io/badge/docker-ready-brightgreen.svg)](infrastructure/docker-compose.yml)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue.svg)](https://www.postgresql.org/)
[![Redis](https://img.shields.io/badge/Redis-7-red.svg)](https://redis.io/)
[![NATS](https://img.shields.io/badge/NATS-2.11-green.svg)](https://nats.io/)

Современная микросервисная система для управления бронированием аудиторий в Санкт-Петербургском политехническом университете.

## 📋 Описание

Сервис бронирования аудиторий предназначен для эффективного управления учебными помещениями университета. Система позволяет студентам и преподавателям бронировать свободные аудитории, избегая конфликтов с существующим расписанием.

### Ключевые возможности

- 🔐 **Многоуровневая авторизация**: студенты, преподаватели, администраторы
- 📅 **Управление бронированиями**: создание, обновление, отмена бронирований
- 🏢 **Каталог аудиторий**: информация о зданиях, аудиториях и их возможностях
- ⚡ **Предотвращение конфликтов**: проверка пересечений бронирований на уровне БД
- 📊 **Интеграция с расписанием**: импорт существующего расписания занятий
- 🔔 **Система уведомлений**: оповещения о статусе бронирований через NATS
- 💾 **Кэширование**: быстрый доступ к данным через Redis

## 🏗️ Архитектура

Проект построен на базе современного микросервисного стека:

```
┌─────────────────┐
│   Application   │
└────────┬────────┘
         │
    ┌────┴────┬─────────┬──────────┐
    │         │         │          │
┌───▼───┐ ┌──▼──┐  ┌───▼────┐ ┌──▼───┐
│ NATS  │ │Redis│  │Postgres│ │Nginx │
│Stream │ │ 7.x │  │  16.x  │ │(soon)│
└───────┘ └─────┘  └────────┘ └──────┘
```

### Используемые технологии

- **PostgreSQL 16** — основная реляционная база данных с поддержкой GIST индексов для предотвращения пересечений бронирований
- **Redis 7** — in-memory кэш для хранения часто запрашиваемых данных
- **NATS 2.11 + JetStream** — система обмена сообщениями для событийно-ориентированной архитектуры
- **Docker & Docker Compose** — контейнеризация и оркестрация сервисов
- **Makefile** — автоматизация развертывания и управления

## 🚀 Быстрый старт

### Предварительные требования

Убедитесь, что у вас установлены:

- [Docker](https://docs.docker.com/get-docker/) (версия 20.10+)
- [Docker Compose](https://docs.docker.com/compose/install/) (версия 2.0+)
- [Make](https://www.gnu.org/software/make/) (опционально)

### Установка и запуск

1. **Клонируйте репозиторий**

```bash
git clone https://github.com/Reivis0/spbstu-room-booking.git
cd spbstu-room-booking
```

2. **Настройте переменные окружения**

```bash
cd infrastructure
cp .env.local .env
```

Отредактируйте `.env` файл, установив безопасные пароли для production окружения.

3. **Запустите инфраструктуру**

С использованием Make:
```bash
cd infrastructure
make build
make up
```

Или напрямую через Docker Compose:
```bash
cd infrastructure
docker-compose build
docker-compose up -d
```

4. **Проверьте статус сервисов**

```bash
make ps
# или
docker-compose ps
```

Все контейнеры должны иметь статус `Up` и `healthy`.

### Доступ к сервисам

После успешного запуска сервисы доступны по следующим портам:

| Сервис     | Порт | Описание                          |
|------------|------|-----------------------------------|
| PostgreSQL | 5432 | База данных                       |
| Redis      | 6379 | Кэш                               |
| NATS       | 4222 | Client connections                |
| NATS HTTP  | 8222 | Monitoring и healthcheck          |
| NATS       | 6222 | Cluster connections               |

## 📊 Модель данных

### Основные сущности

#### Users (Пользователи)
- Роли: `student`, `teacher`, `admin`
- Аутентификация и авторизация
- Профильная информация

#### Buildings (Здания)
- Код здания
- Название и адрес
- Привязка к аудиториям

#### Rooms (Аудитории)
- Номер и код аудитории
- Вместимость
- Технические характеристики (JSONB)

#### Bookings (Бронирования)
- Статусы: `pending`, `confirmed`, `cancelled`, `rejected`
- Временные слоты с проверкой пересечений
- Привязка к пользователю и аудитории

#### Schedule Import (Импорт расписания)
- Интеграция с внешними источниками расписания
- Хэширование для отслеживания изменений
- Метаданные импорта

### Защита от конфликтов

Система использует GIST-индексы PostgreSQL для предотвращения пересечений бронирований:

```sql
EXCLUDE USING GIST (
    room_id WITH =,
    tstzrange(starts_at, ends_at) WITH &&
)
```

## 🔄 Event-Driven Architecture

Проект использует NATS JetStream для обработки событий:

### Streams

| Stream             | Subjects                              | Назначение                           |
|--------------------|---------------------------------------|--------------------------------------|
| booking_events     | booking.{created,updated,cancelled}   | События бронирований                 |
| schedule_events    | scheduling.imported                   | События импорта расписания           |
| cache_invalidate   | cache.invalidate.room                 | Инвалидация кэша                     |
| notifications      | notification.trigger                  | Триггеры для отправки уведомлений    |

### Конфигурация

- **Storage**: file (persistent) для критичных событий, memory для кэша
- **Retention**: от 10 минут до 24 часов в зависимости от типа события
- **Max messages**: 1000-5000 сообщений на stream

## 🛠️ Управление

### Команды Make

```bash
# Сборка всех сервисов
make build

# Запуск инфраструктуры
make up

# Остановка всех сервисов
make down

# Просмотр статуса
make ps

# Просмотр логов
make logs
```

### Работа с базой данных

Подключение к PostgreSQL:
```bash
docker exec -it booking-postgres psql -U booking_user -d booking_service
```

### Работа с Redis

Подключение к Redis CLI:
```bash
docker exec -it booking-redis redis-cli -a secure_password
```

### Работа с NATS

Просмотр streams:
```bash
docker exec -it booking-nats nats stream list
```

Просмотр информации о stream:
```bash
docker exec -it booking-nats nats stream info booking_events
```

## 📈 Мониторинг и healthchecks

Все сервисы имеют настроенные healthcheck'и:

- **PostgreSQL**: `pg_isready` проверка каждые 10 секунд
- **Redis**: `redis-cli ping` каждые 10 секунд  
- **NATS**: HTTP healthcheck на `/healthz` каждые 10 секунд

Проверить здоровье NATS:
```bash
curl http://localhost:8222/healthz
```

## 🔧 Конфигурация

### Переменные окружения

Основные переменные в `.env` файле:

```env
# PostgreSQL
POSTGRES_DB=booking_service
POSTGRES_USER=booking_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_password

# NATS
NATS_CLIENT_PORT=4222
NATS_HTTP_PORT=8222
NATS_CLUSTER_PORT=6222

# Application
APP_PORT=8080
APP_ENV=production
```

### Ресурсы контейнеров

Для каждого сервиса настроены лимиты ресурсов:

| Сервис     | CPU Limit | Memory Limit | CPU Reserve | Memory Reserve |
|------------|-----------|--------------|-------------|----------------|
| PostgreSQL | 2 CPU     | 2GB          | 0.5 CPU     | 512MB          |
| Redis      | 1 CPU     | 512MB        | 0.25 CPU    | 256MB          |
| NATS       | 1 CPU     | 1GB          | 0.25 CPU    | 256MB          |

## 📝 Миграции базы данных

Миграции применяются автоматически при первом запуске PostgreSQL контейнера через `docker-entrypoint-initdb.d`.

Расположение миграций: `infrastructure/postgreSQL/migrations/`

- `1_init.up.sql` — создание схемы БД
- `1_init.down.sql` — откат миграции
- `1_filling.sql` — начальное заполнение данными

## 🐛 Отладка

### Просмотр логов

Все логи сервисов:
```bash
docker-compose logs -f
```

Логи конкретного сервиса:
```bash
docker-compose logs -f postgres
docker-compose logs -f redis
docker-compose logs -f nats
```

### Перезапуск сервиса

```bash
docker-compose restart postgres
```

### Пересборка после изменений

```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 🔒 Безопасность

- ⚠️ **Важно**: Измените дефолтные пароли в `.env` перед production деплоем
- Используйте сильные пароли для PostgreSQL и Redis
- Ограничьте доступ к портам сервисов через firewall
- Регулярно обновляйте Docker образы для получения security патчей

## 📚 Структура проекта

```
spbstu-room-booking/
├── infrastructure/               # Инфраструктурные компоненты
│   ├── docker-compose.yml       # Оркестрация сервисов
│   ├── Makefile                 # Команды управления
│   ├── .env.local               # Пример переменных окружения
│   ├── postgreSQL/              # PostgreSQL конфигурация
│   │   ├── Dockerfile
│   │   ├── postgresql.conf      # Конфигурация PostgreSQL
│   │   ├── pg_hba.conf          # Настройки доступа
│   │   └── migrations/          # SQL миграции
│   ├── redis/                   # Redis конфигурация
│   │   ├── Dockerfile
│   │   └── redis.conf
│   └── nats/                    # NATS конфигурация
│       ├── Dockerfile
│       ├── nats.conf
│       └── init_streams.sh      # Инициализация streams
└── README.md                    # Документация проекта
```

## 🤝 Участие в разработке

Мы приветствуем вклад в развитие проекта! 

### Процесс контрибуции

1. Форкните репозиторий
2. Создайте feature-ветку (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👥 Авторы

- **Reivis0** - *Разработка и поддержка* - [GitHub](https://github.com/Reivis0)

## 📞 Контакты

Если у вас есть вопросы или предложения, пожалуйста:

- Откройте [Issue](https://github.com/Reivis0/spbstu-room-booking/issues)
- Напишите в [Discussions](https://github.com/Reivis0/spbstu-room-booking/discussions)

## 🗺️ Roadmap

- [ ] Добавление REST API для управления бронированиями
- [ ] Интеграция с системой аутентификации СПбПУ
- [ ] Web-интерфейс для пользователей
- [ ] Мобильное приложение
- [ ] Система уведомлений (Email, Telegram)
- [ ] Интеграция с системой расписания университета
- [ ] Nginx reverse proxy (в процессе)
- [ ] Метрики и мониторинг (Prometheus + Grafana)
- [ ] Резервное копирование данных

---

<div align="center">
Сделано с ❤️ для СПбПУ
</div>
